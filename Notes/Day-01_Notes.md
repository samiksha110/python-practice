1) What is the difference between = and ==?
>  = assigns (copies) a value into a variable, while == compares whether two values are equal.

2) What does % do?
> % acts as modulus operator (10 % 3 results 1 so 3 goes into 10 3times, leaving remainder of 1) 

3) Why do we use int()?
> input() always returns a string. We use int() to convert that string into an integer so we can perform mathematical operations.

4) Why didn't we use int() for the operator?
> Operators like +, -, *, / are characters (strings), so we don't convert them to integers.

5) What was the hardest program today?
> For me its was the swapping of numbers , i was confused about assignment operatore and temp .

## Mistakes I Made Today

- I confused = with ==
- I thought variables stayed connected after assignment
- I forgot to compare a with c in Largest of Three